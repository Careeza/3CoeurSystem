#include "editMap.h"

void    loadBigTree01(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);
    assetSelected = assets->loadAsset(BigTree01, 1, 40, YBigTree01);

    haveAnAsset = true;
}

void    loadBigTree02(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);
    assetSelected = assets->loadAsset(BigTree02, 1, 40, YBigTree02);
    haveAnAsset = true;
}

void    loadBigTree03(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(BigTree03, 1, 40, YBigTree03);
    haveAnAsset = true;
}

void    loadBushTree01(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(BushTree01, 1, 40, YBushTree01);
    haveAnAsset = true;
}

void    loadBushTree02(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(BushTree02, 1, 40, YBushTree02);
    haveAnAsset = true;
}

void    loadBushTree03(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(BushTree03, 1, 40, YBushTree03);
    haveAnAsset = true;
}

void    loadBushTree04(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(BushTree04, 1, 40, YBushTree04);
    haveAnAsset = true;
}

void    loadGrass01(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(Grass01, 1, 40, YGrass01);
    haveAnAsset = true;
}

void    loadGrass02(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(Grass02, 1, 40, YGrass02);
    haveAnAsset = true;
}

void    loadGrass03(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(Grass03, 1, 40, YGrass03);
    haveAnAsset = true;    
}

void    loadGrass04(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(Grass04, 1, 40, YGrass04);
    haveAnAsset = true;
}

void    loadGrass05(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(Grass05, 1, 40, YGrass05);
    haveAnAsset = true;
}

void    loadGround01(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(Ground01, 1, 40, YGround01);
    haveAnAsset = true;
}

void    loadMediumTree01(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(Plateforme, 1, 40, YMediumTree01);
    haveAnAsset = true;
}

void    loadMediumTree02(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(Dirt, 1, 40, YMediumTree02);
    haveAnAsset = true;
}

void    loadMediumTree03(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(MediumTree03, 1, 40, YMediumTree03);
    haveAnAsset = true;
}

void    loadSmallTree01(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(SmallTree01, 1, 40, YSmallTree01);
    haveAnAsset = true;    
}

void    loadSmallTree02(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(SmallTree02, 1, 40, YSmallTree02);
    haveAnAsset = true;
}

void    loadSmallTree03(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(SmallTree03, 1, 40, YSmallTree03);
    haveAnAsset = true;
}

void    loadSmallTree04(CS_Asset* &assetSelected, CS_Assets *assets, bool& haveAnAsset)
{
    changeButton(assetSelected, assets, haveAnAsset);    
    assetSelected = assets->loadAsset(SmallTree04, 1, 40, YSmallTree04);
    haveAnAsset = true;
}